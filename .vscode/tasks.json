{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build",
            "type": "shell",
            "command": "cmake --build cmake-build-debug",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}